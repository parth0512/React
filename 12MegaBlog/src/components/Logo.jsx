import React from "react";

function Logo({ width = "100px" }) {
  return (
    <div
      style={{
        fontFamily: "Pinyon Script",
        fontSize: "40px",
        fontWeight: "bold",
      }}
    >
      Fuse
    </div>
  );
}

export default Logo;
